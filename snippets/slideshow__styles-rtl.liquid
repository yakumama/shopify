<style media="{{ media_condition }}">
    #shopify-section-{{ section.id }} .slider-button--next {
        left: 0;
        right: auto;
    }

    @media (min-width: 990px) {
        {% if section.settings.heading != blank and per_view > 1 %}

        #shopify-section-{{ section.id }} .slider-buttons {
            left: 0;
            right: auto;
        }{% endif %}
    }
</style>
